# TradeSetu Stock Analyzer — dependencies.
# All installed into the shared project .venv (see ../requirements.txt).
# Listed here so this folder is self-contained if copied elsewhere.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0

langchain>=1.3.0
langchain-core>=1.4.0
langchain-openai>=1.3.0
langchain-tavily>=0.2.0
langgraph>=1.2.0

yfinance>=1.5.0
pandas>=2.0.0
